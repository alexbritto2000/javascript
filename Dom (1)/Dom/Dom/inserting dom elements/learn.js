const foodContainerEl = document.getElementById("food-container");

let rasam=document.getElementById('rasam')

const li = document.createElement("li");
li.textContent = "Biriyani";
li.className = "food-item";

// foodContainerEl.append(li)

// foodContainerEl.prepend(li)

// foodContainerEl.after(li)

// foodContainerEl.before(li)

// rasam.after(li)

//oldway

// console.log(foodContainerEl.parentNode.insertBefore(li,foodContainerEl))

// foodContainerEl.parentNode.insertBefore(li,foodContainerEl.nextSibling)

// console.log(foodContainerEl.nextSibling)


//innerHTMl

// foodContainerEl.insertAdjacentHTML('afterbegin','fooditems')

// foodContainerEl.insertAdjacentHTML('afterend','fooditems')

// foodContainerEl.insertAdjacentHTML('beforebegin','beforebegin')

// foodContainerEl.insertAdjacentHTML('beforeend','beforeend')









